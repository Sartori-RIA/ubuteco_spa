import {Dictionary} from '@ngrx/entity';
import {WineStyle} from '../../core/models/wine-style';
import {Table} from '../../core/models/table';

export const tablesDictionary: Dictionary<Table> = {
};
